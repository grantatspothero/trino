remote exchange (GATHER, SINGLE, [])
    local exchange (GATHER, UNKNOWN, [])
        remote exchange (REPARTITION, ROUND_ROBIN, [])
            join (INNER, PARTITIONED):
                join (INNER, PARTITIONED):
                    single aggregation over (d_week_seq)
                        final aggregation over (d_day_name, d_week_seq)
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, ["d_week_seq"])
                                    partial aggregation over (d_day_name, d_week_seq)
                                        join (INNER, REPLICATED):
                                            local exchange (REPARTITION, ROUND_ROBIN, [])
                                                alternatives
                                                    dynamic filter (await ["ws_sold_date_sk"])
                                                        scan web_sales
                                                    cache data
                                                        dynamic filter (await ["ws_sold_date_sk"])
                                                            scan web_sales
                                                    load from cache
                                                alternatives
                                                    dynamic filter (await ["cs_sold_date_sk"])
                                                        scan catalog_sales
                                                    cache data
                                                        dynamic filter (await ["cs_sold_date_sk"])
                                                            scan catalog_sales
                                                    load from cache
                                            local exchange (GATHER, SINGLE, [])
                                                remote exchange (REPLICATE, BROADCAST, [])
                                                    alternatives
                                                        dynamic filter (["d_week_seq"], await ["d_week_seq"])
                                                            scan date_dim
                                                        cache data
                                                            dynamic filter (["d_week_seq"], await ["d_week_seq"])
                                                                scan date_dim
                                                        load from cache
                    local exchange (GATHER, SINGLE, [])
                        remote exchange (REPARTITION, HASH, ["d_week_seq_22"])
                            alternatives
                                dynamic filter (["d_week_seq_22"])
                                    scan date_dim
                                cache data
                                    dynamic filter (["d_week_seq_22"])
                                        scan date_dim
                                load from cache
                local exchange (GATHER, SINGLE, [])
                    remote exchange (REPARTITION, HASH, ["expr_226"])
                        join (INNER, PARTITIONED):
                            single aggregation over (d_week_seq_129)
                                final aggregation over (d_day_name_139, d_week_seq_129)
                                    local exchange (GATHER, SINGLE, [])
                                        remote exchange (REPARTITION, HASH, ["d_week_seq_129"])
                                            partial aggregation over (d_day_name_139, d_week_seq_129)
                                                join (INNER, REPLICATED):
                                                    local exchange (REPARTITION, ROUND_ROBIN, [])
                                                        alternatives
                                                            dynamic filter (await ["ws_sold_date_sk_51"])
                                                                scan web_sales
                                                            cache data
                                                                dynamic filter (await ["ws_sold_date_sk_51"])
                                                                    scan web_sales
                                                            load from cache
                                                        alternatives
                                                            dynamic filter (await ["cs_sold_date_sk_88"])
                                                                scan catalog_sales
                                                            cache data
                                                                dynamic filter (await ["cs_sold_date_sk_88"])
                                                                    scan catalog_sales
                                                            load from cache
                                                    local exchange (GATHER, SINGLE, [])
                                                        remote exchange (REPLICATE, BROADCAST, [])
                                                            alternatives
                                                                dynamic filter (await ["d_week_seq_129"])
                                                                    scan date_dim
                                                                cache data
                                                                    dynamic filter (await ["d_week_seq_129"])
                                                                        scan date_dim
                                                                load from cache
                            local exchange (GATHER, SINGLE, [])
                                remote exchange (REPARTITION, HASH, ["d_week_seq_175"])
                                    alternatives
                                        scan date_dim
                                        cache data
                                            scan date_dim
                                        load from cache
